# --- FREE MODE (default) ---
# Optional: override the default public Hugging Face Space used for Image+Prompt → Video
# (This is public and safe to expose, hence NEXT_PUBLIC_)
# NEXT_PUBLIC_HF_SPACE_ID=multimodalart/stable-video-diffusion

# --- OPTIONAL: REPLICATE MODE ---
# Replicate API Token (required only if you select "Replicate" in the UI)
# REPLICATE_API_TOKEN=YOUR_REPLICATE_TOKEN_HERE

# Replicate model slug = {owner}/{name}
# Example (paid model): minimax/video-01-director
# REPLICATE_MODEL_OWNER=minimax
# REPLICATE_MODEL_NAME=video-01-director
